// The users layout component is the root component of the users 
// feature / section of the app, it binds the component to the 
// users layout template with the templateUrl property of the 
// angular @Component decorator.

import { Component } from '@angular/core';

@Component({ templateUrl: 'layout.component.html' })
export class LayoutComponent { }